# Small JVM build-time plugin for generated version metadata and developer build information.
# It has no runtime footprint in the native application.
product: jvm/amper-plugin

settings:
  kotlin:
    version: 2.4.10
