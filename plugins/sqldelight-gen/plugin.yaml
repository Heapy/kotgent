tasks:
  generate:
    action: !io.kotgent.sqldelight.generateDatabase
      sqDir: ${module.rootDir}/sqldelight
      generatedSourceDir: ${taskOutputDir}

generated:
  sources:
    - language: kotlin
      directory: ${tasks.generate.action.generatedSourceDir}
